# 开发环境：留空 = 同源（走 vite proxy 到 NestJS 网关 3000，再由网关转发到 FastAPI 8000）
# 不要填 http://127.0.0.1:8000 —— 直连 8000 会绕过网关鉴权，本地/线上链路就不一致了。
# 部署时若前后端分离部署，改为网关对外地址，如 https://api.example.com
VITE_API_BASE=

# 可选：改掉 vite proxy 的目标（默认 http://127.0.0.1:3000，见 vite.config.ts）
# VITE_DEV_API_TARGET=http://127.0.0.1:3000
